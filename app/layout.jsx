export const metadata = {
  title: 'Master Organization Mapper',
  description: 'Premium Web App for Mapping Organizational Codes',
}

import './globals.css'

export default function RootLayout({ children }) {
  return (
    <html lang="id">
      <body>{children}</body>
    </html>
  )
}
